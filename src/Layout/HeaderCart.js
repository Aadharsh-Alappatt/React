import CartIcon from "../Cart/CartIcon";

const HeaderCart = (props) => {
  return (
    <button>
      <span>
        <CartIcon />
      </span>
      <span></span>
      <span></span>
    </button>
  );
};

export default HeaderCart;
